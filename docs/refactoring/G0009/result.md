# G0009 实施结果

日期：2026-09-12。状态：本地与独立 Debug 暂存检索已验证，真实 Host 待验。
基线 `9539b54`，G0008 的 134 项测试。

## 已完成

- G0009-01：实体、别名、关联实体身份、类型化事实候选与修订来源已实现，正式/工作状态继续由修订指针决定。
- G0009-02：有界中文文本与别名检索已实现；不依赖 FTS 分词，结果保留章节和修订来源。
- G0009-03：上下文清单、必带规范/锁定状态/有效事实、可选项预算与省略说明已实现。
- G0009-04：跨书/过期/未知实体/旧值/证据/时间状态/锁定冲突门禁，提交与定稿前再次校验已实现。
- G0009-05：中文故事夹具、持久化重开、schema 6 升级备份、原生实体输入与上下文预览回归已实现。

## 实际验证

本地入口完成锁定还原、Debug 零警告构建、164 项测试（无跳过）、格式检查和文档链接检查。
TRX：`artifacts/G0009/tests/g0009.trx`。原生截图：`artifacts/G0009/ui/story-context.png`，已检查实体表单输入和窗口布局；
较低区域可通过滚动访问。既有明暗编辑、模板、连接和关闭用例继续通过。

最终 Debug 暂存目录为 `artifacts/G0009/stage-final-dcc0308a52874602910e28c28bfb3a7c/NovelGeneratePlugin`（11 文件）。
独立 ALC 加载原生 SQLite、创建/提交/保存/重开中文工作稿，林舟与阿舟分别唯一命中。
探针使用实际领域和存储程序集，不借用测试 bin；没有生成发布 ZIP 或修改真实 Host。

GPT-6 文本审查报告在 `artifacts/G0009/codex-review/review.md`。修正其发现的事实验证后规范变化仍能接受、锁定字段改名覆盖另一字段，
并新增回归。还验证字典顺序变化不影响重开指纹、检索达到扫描容量明确停止、关联身份进入事实键。
一次原生 VM 回归发现规范更新未立即使预览失效，已修复并复跑通过。

## 限制

没有新增真实模型生成请求，语义抽取准确率未由本阶段证明。时间状态由后续任务明确提供并检查，复杂倒叙和知情推演仍归 G0017。
实体删除、多项锁定完整编辑、长篇高效检索未在此阶段开放，见 [记忆契约](memory-and-context-contract.md) 与 [检索夹具](retrieval-fixtures.md)。
真实 Host 的完整 Dock/Dispatcher 联调待 G0015。没有 AIFLOW、Windows CI、Release/ZIP 或发布门禁。

下一阶段 G0010：用既有请求入口和上下文生成全书主线、分卷目标与近期章节计划。
