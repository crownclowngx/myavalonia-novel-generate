# G0015 阶段计划

日期：2026-09-12。基线 `2a28405`，262 项本地测试。

按 G0015-01–05 组织正文工作区与侧栏、新建空状态、共享工具和七项宿主命令。
原生控件继续直接调用现有用例；不嵌入 HTML，不访问 Host 私有 Dock。验证长文本、中文输入、明暗主题、窄窗和缩放。

补齐命令异步等待、外部取消、双书隔离、关闭排空与迟到通知回归。真实 Host 证据与 Headless 分别记录。
不执行 AIFLOW、Windows CI、Release/ZIP 或发布门禁。
