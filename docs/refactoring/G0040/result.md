# G0040 实施结果

日期：2026-09-13。状态：本地已验证，5/5 工作项完成。

已复现用户反馈节点的真实契约错误：$.Entities[1].Evidence 为 11 条，上限为 8。响应已完整结束，27417 为已知输入输出合计，不能通过增加总预算解决。

## 验证证据

- 27 项专项测试通过；新增 7 个用例覆盖三类 Evidence 的 8/9 边界、新 Schema 容量与段号、具体错误规则、一次纠正的诊断传递，以及升级修订复用 v4 成功成果。
- tools/verify-local.ps1 通过：锁定还原、Debug 构建 0 警告/错误、409 项测试通过、格式校验及文档来源校验通过。
- 1.1.2 Release 构建 0 警告/错误，409 项 Release 测试全部通过；11 个筛选暂存文件的独立加载与 SQLite 存储探针通过。
- 真实候选只读回放精确定位上述字段；121 个可引用段落中对应原文坐标仍由本地核对。
- 最新运行 11 个成功节点和历史运行 3 个成功节点的输入指纹、保存 JSON 哈希、契约复核全部通过。
- 回放期间网络调用为 0，正式工作区三库只读打开，原始候选与历史费用未改写。

本机证据在忽略目录 artifacts/G0040/node-20260913-173139、candidate-replay、cache-replay。原文、数据库与候选不提交 Git；开发工具只输出数值、固定字段路径和回放范围说明。

新增模型约束的真实成功率仍需后续实际使用观察；离线回放没有重新调用模型。1.1.2 筛选产物已在 artifacts/deployments/20260913-174700-contract-diagnostics/staged/NovelGeneratePlugin 准备完成，实际覆盖前需退出仍在运行的宿主；部署状态单独记录。
