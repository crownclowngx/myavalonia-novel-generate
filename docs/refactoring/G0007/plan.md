# G0007 备份导出与 P0 本地验收

基线 G0006 `507f2fe`，84 项测试通过。

执行 G0007-01 至 G0007-04：一致性作品备份与保留身份恢复、独立模板文件导入导出、
编辑稿/工作稿视图/正式稿 TXT 与 Markdown 按章范围导出、所选版本本地复查、窄模型端口及测试 Fake。
输出使用新文件和原子提交，已有路径不覆盖；失败保留旧输出。作品、模板与普通连接配置不携带 Key。

G0007-05 区分 Standalone/原生控件/开发资产证据与真实 Host；Host 未验证的格子保留待验，不以 Headless 冒充。
只读核对发现目标 Host 同步释放容器，故将 G0015 的必要同步关闭/异步 Shutdown 适配提前纳入本阶段；不修改 Host。
不使用 AIFLOW、Windows CI 或发布门禁。
