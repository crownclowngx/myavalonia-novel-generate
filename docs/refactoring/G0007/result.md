# G0007 实施结果

日期：2026-09-12。状态：P0 本地已验证，真实 Host 待验。

- G0007-01：一致性作品备份、保留身份恢复和单模板 JSON 独立导入导出已实现。
- G0007-02：编辑稿/工作稿视图/正式稿按章范围导出 TXT 与 Markdown，实际选定正文复查、提示确认、过期预览保护已实现。
- G0007-03：活跃 WAL、未提交事务、恢复迁移、无效/占用路径、现有文件保护、模板源变化与隔离已有回归。
- G0007-04：窄模型端口与测试专用 ScriptedTextModel 已建立；生产未注册替身，尚未提供小说生成。
- G0007-05：原生控件和标准 DI 的本地场景已验证；真实 Host Dock/重开/重启仍待验，见 [P0 矩阵](p0-acceptance-matrix.md)。

## 实际验证

99 项测试通过、无跳过；TRX 在 `artifacts/G0007/tests/g0007.trx`。本地入口完成锁定还原、Debug 零警告构建、
全部测试、格式与文档链接检查。原生控件截图在 `artifacts/G0007/ui/`。

导出源码的 GPT-6 Astra 文本审查未发现可独立复现缺陷，结论限于所提供文件；报告保存为 `export-review.md`。
随后对新增关闭适配及模型进行补充审查，修正忙碌时模板切换导致错误发布、密钥保存失败丢输入，均新增回归。
关闭收尾失败不再把已进入释放的对象重新展示成可编辑状态；前置保存失败仍保留编辑能力。
相应报告在 `artifacts/G0007/codex-review/`，不以模型意见代替实际测试。

## 提前处理与限制

核对 Host `ea96b72` 发现其同步释放 Scope/Provider，提前实现 G0015 必要的生命周期适配：
同步 Dispose 启动受跟踪任务，SDK Shutdown 等待后才释放根会话。原生 UI 线程上的实际 DI 测试验证最后正文/模板输入落盘，
并覆盖重复关闭、在途等待、未显示 Tool 和失败重试。没有修改 Host 工作树。

真实 Host 的 Dispatcher/Dock 完整联调未完成，不能称为 Host 已验收；P0 仅本地完成。
没有使用 AIFLOW、Windows CI、Release、ZIP 或发布门禁。没有真实 DeepSeek 调用，也没有小说生成结果。
阶段独立提交后进入 G0008 Codex/DeepSeek 模型协议与用量基础。
