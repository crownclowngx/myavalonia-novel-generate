# G0011 阶段计划

基线：`a5fa71a`，G0010 完成，184 项本地测试通过。日期：2026-09-12。

按总计划实施单章流式候选、规则/事实/剧情方法/文风审校、最多两轮局部修正与全量复查、原子工作稿提交。
模型统一走已有请求预算入口；候选与作者编辑分离并可重开查看；单章调度将供 G0012 复用。

验证包括故障模型、真实 SQLite、版本变化、取消/迟到、提交中断、事实证据、有限修复新问题和原生绑定。
实际模型使用 Codex GPT-6；不运行 Windows CI、发布门禁，不使用 AIFLOW。遵循 SOLID、朴素组合与中文设计注释。
