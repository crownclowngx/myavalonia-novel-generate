# G0011 实施结果

日期：2026-09-12。基线 `a5fa71a`，G0010 的 184 项测试。

G0011-01–05 已实现单章候选与恢复、四维审校、有限局部修正/全量复查、本地格式修复、原子工作稿提交及失败/取消/迟到路径。
生产工程保持单程序集，使用普通记录、窄接口和组合。原生窗口独立显示候选、报告，支持问题证据定位和提交。

## 验证记录

测试覆盖真实 SQLite 事务、故障模型、作者输入与候选隔离、重开恢复、规范过期、检查未完成、事实与字数门禁、
修正范围/唯一性、修正引入新问题、取消迟到、提交失败重试、并发编辑合并、通知失败隔离和候选序列。
TRX 为 `artifacts/G0011/tests/g0011.trx`，原生截图 `artifacts/G0011/ui/chapter-generation.png` 已检查。
最终本地数量、独立暂存探针与最新模型样本在阶段收尾补充。

GPT-6 静态审查 `artifacts/G0011/codex-review/review.md` 的四个发现均修复并增加回归：无关编辑使生成正文倒退、通知异常阻断保存关闭、
旧候选覆盖新版本、重叠片段误判唯一。未用审查结论替代实际测试。

实际 Codex CLI 0.153.4、gpt-6-astra 首次样本：目标 600 字，允许一次局部修正，完成生成/审校/修正/复查四次请求。
最终 689 字，有实体关系疑点，停在 NeedsAttention；没有提交工作稿。四次请求共 55243 token，报告与候选保存于 `artifacts/G0011/live-probe`。
复核后将背景用品登记要求收窄为影响后续身份/状态的实体，仍保留未决事实阻塞；第二个限额样本单独记录。

完整连续运行、自动采用下一章及运行恢复归 G0012；真实 Host 全流程归 G0015。没有 AIFLOW、Windows CI、Release/ZIP 或发布门禁。

最终本地门禁：211 项测试全部通过、无跳过；锁定还原、Debug 零警告构建、格式检查和 238 条文档链接通过。
独立暂存 `artifacts/G0011/stage-final-abfaa2a2d2374485b11634f811295373/NovelGeneratePlugin`（11 文件）的 ALC/SQLite 中文保存重开与检索探针通过。

第二个限额样本（零自动修正、最多两条请求）生成 622 字，通过一次完整审校并原子提交为一条 Passed 工作稿，正式指针为空。
两条请求实际共 26572 token。证据位于 `artifacts/G0011/live-probe-refined/bin/Debug/net10.0` 的 chapter.json、requests.json 和单章实测.noveldb。
此次成功说明协议与单章业务可调用闭环，不代表复杂长篇稳定性或自动审校准确率已经达标。
