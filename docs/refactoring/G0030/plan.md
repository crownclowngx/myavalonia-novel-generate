# G0030 阶段计划

日期：2026-09-12。状态：阶段已验收，见[结果](result.md)。基线：`754b0f4`，299 项本地测试通过。

承接[专项 G0030](../../novel-analysis-feasibility-plan.md#g0030)。实现 G0030-01–05：有界 TXT 读取与严格编码预览、标题分章/无标题退化、原始坐标与哈希、正文/上下文切块、实际文件与取消/重开验证。

文件读取通过窄端口隔离，确定性分章切块放在独立纯算法中，应用用例编排预览与确认导入。预览不保存书目、不调用模型；确认保存的是预览快照。保留所有换行和标题，排除章节必须由调用方明确选择，不自动删广告或目录。

验证包含 UTF-8/UTF-16/UTF-32 BOM、GB18030、错误编码、空文件、非 TXT、超限、重复章名、长标题、无标题、超长章、代理对、全部正文覆盖和取消。实际用户文件仅在本机读取并验证元数据，不进入公开夹具。性能按 G0029 固定口径记录。
