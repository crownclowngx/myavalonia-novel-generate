# G0030 实施结果

日期：2026-09-12。状态：阶段已验收。基线：`754b0f4`，299 项测试。

G0030-01–05 完成：严格有界 TXT 读取与编码预览，确定性分章/无标题片段，原始坐标和代理对边界，完整单元覆盖，异步预览与不可变快照确认导入。实际职责见[方案](implementation.md)，行为约定见[来源与覆盖契约](source-and-coverage-contract.md)。

## 实际验证

- 新增 `NovelImportTests` 14 项通过，覆盖七组编码、损坏/BOM 冲突、双容量限制、长章/重复标题/卷首、代理对、取消和源文件变化。
- `tools/verify-local.ps1` 全部通过：Debug 0 警告/0 错误，313 项测试、0 失败、0 跳过；锁定还原、格式、文档链接/HTML 来源哈希和 Git 空白检查通过。
- `AnalysisProbe import` 对用户所给 TXT 实测：147,972 字节，51,071 字符，20 个章节/片段、20 个单元，覆盖 51,071 字符。导入约 110 ms，重开约 11 ms。来源哈希与 G0029 核查一致。
- `AnalysisProbe benchmark` 三次处理 200 万字符合成文本：1,241 个章节/单元，覆盖完整；导入约 211/111/117 ms，重开约 53/43/44 ms；采样托管内存增量约 40.4/30.2/31.9 MiB。该规模只验证本地导入容量，不证明模型预算或长篇质量。
- 实机：Windows x64，14 个逻辑处理器，.NET 10.0.10。原始本机结果位于 `artifacts/G0030/user-import/import-results.json` 与 `artifacts/G0030/capacity/import-results.json`，目录未进入生产包或 Git。

## 范围与下一步

本阶段未执行真实模型请求、原生界面交互、Windows CI 或发布门禁。普通开发工具不自动纳入付费测试。用户 TXT 的完整文件已通过导入验证，原小说其他章节未知；百万字符真实文学质量仍未验证。

下一阶段 G0031：接入 DeepSeek Flash 的有界多维提取和精确原文证据。累计 2/8 阶段、10/40 工作项，M1 尚未达到。
