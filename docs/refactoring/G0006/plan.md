# G0006 长期规则与确定性检测

基线 G0005 `dba8c5c`，73 项测试通过。

执行 G0006-01 至 G0006-04：版本化原文/解释/来源、通用快照/本书/本卷/本次运行范围、强弱与启停，
有限的禁止文本/必须包含/语义指导三种规则，跨标点换行可选、例外文本覆盖、原文命中位置与冲突报告。
本书规则表单草案也持久化；检查记录绑定正文摘要和规则摘要，旧结果自动显示过期。

规则界面不自动改写正文。本阶段不把语义指导标为已检查，也不标记任何规则已用于模型请求。
验证纯中文夹具、范围、例外、冲突、重启与无网络操作；不使用 AIFLOW、Windows CI、发行门禁。
