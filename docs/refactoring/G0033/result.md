# G0033 阶段结果

日期：2026-09-13。状态：工程已验证，5/5 项；真实整合继续执行。

已实现五项工作对应的索引、身份候选与保守采纳、规则/事件/目标整合、时序和冲突传播、统一预算与恢复。新增 13 项测试覆盖别名与同名异人、遗漏/重复/越界映射、不确定分组、四类非客观来源、规则例外、倒叙、阶段追加、缓存恢复和高密度分批，均已通过。

用户文件提取已全覆盖：20 单元、51071 字符、324 条提及、542 条事实/观察、108 条信息缺口。24 次真实提取请求中 20 次通过、4 次候选拒绝，全部已知用量共 252787 token。最后一次拒绝为 Gaps 出现额外 Statement 字段；没有删除字段伪造成功，明确重试后完成末单元。

真实整合修订已复用全部 20 个提取节点，未重新发送章节提取；依据实际密度追加 18 个整合节点，沿用同一预算。目前运行中，最终用量和结果待追加。全书语义准确率、召回率、文风评价与 Host 交互验收尚未进行，最终目标未达到。

真实整合的 6 个身份节点首先通过；第一个规则节点把人物价值冲突标为 Conflict/Explicit/CharacterClaim，被原先过宽的“所有 Conflict 必须不确定”约束误拒绝。现已区分故事冲突 Conflict 与未消解的事实矛盾 Contradiction；只有后者必须 Uncertain，并增加第 14 项专项回归。跨章解释提示也采用保守的 Inferred/Uncertain。旧候选及 13385 已知 token 保留，新修订复用 20 个提取和 6 个身份结果，仅重算未完成的事实整合。此时同一预算累计 31 次请求、318352 已知 token。

本地完整门禁：354 项通过，0 失败/跳过，编译 0 警告/错误；格式、文档链接、手册源哈希与 Git 空白检查通过。真实修订继续到 31/38 节点时，模型在 Role 字段使用了契约未允许的 Plan；已保留候选，明确恢复时仅重试该节点，未扩大枚举以绕过拒绝。
