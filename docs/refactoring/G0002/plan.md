# G0002 SQLite 项目与可靠保存会话

日期：2026-09-12。前置：G0001 已提交 `ce8f82c`，本地构建、2 项测试及文档检查通过。

按[总计划 G0002](../../implementation-plan.md#g0002)实施 G0002-01 至 G0002-05：

1. 建立项目/卷/章身份、不可变编辑快照和 schema 版本。
2. SQLite 作品库保存完整当前编辑快照，目录库仅登记最近打开元数据；章节修订历史在 G0003 建立。
3. 单书写入租约、进程内身份登记、串行保存及版本校验；拒绝重复写入。
4. 原生新建/打开/保存入口、章节编辑、自动保存、独立恢复副本与关闭排空。
5. 实际文件往返、双书、锁冲突、失败恢复、版本拒绝及私有资产暂存检查；完成后独立 Git 提交。

本阶段不调用模型，不把当前编辑快照称为已定稿，不实现后续模板与模型 Tool。
