# G0002 实施结果

日期：2026-09-12。状态：本地实现与阶段验证完成；真实 Host 联调尚未执行。

## 实现与验证

| 工作项 | 结果 |
| --- | --- |
| G0002-01 | 不可变书卷章、稳定身份、合法性及格式版本已建立；包含非法身份、归属和未知版本测试 |
| G0002-02 | SQLite 作品库/目录库往返、失败建库不登记、目录异常不影响作品、覆盖保护通过 |
| G0002-03 | 路径/书身份去重、系统租约、双书保存、预期版本比较与单写队列通过 |
| G0002-04 | 新建/打开、编辑、自动保存、显式保存、恢复入口、两段关闭及错误状态已接入 |
| G0002-05 | 实际写锁、只读文件、双重保存失败、恢复版本竞争、独立 ALC 私有 SQLite 探针通过 |

`./tools/verify-local.ps1`：锁定还原、Debug 零警告构建、25 项测试（无跳过）、格式及 Markdown 链接检查通过。
测试包括两个实际 Avalonia 控件树场景：1200×850 明色、800×650 暗色，输入中文、切换章节、保存真实文件。
截图位于 `artifacts/G0002/ui/`；它们是原生界面测试渲染，不是真实 Host 截图。

开发暂存含 10 个文件，`tools/probe-staged-storage.ps1` 在独立 ALC 从暂存目录加载 win-x64 SQLite 并创建/读取中文项目。
未运行 Windows CI、Release、正式 ZIP、发布门禁或修改真实 Host 安装目录。

## 代码审查

使用已登录 Codex 的 `gpt-6-astra` 对明确提供的源码文本做真实审查；四项问题已修正并补充竞争/隔离回归。
详见[模型验证边界](model-validation-boundary.md)。审查不能替代测试，未成功执行的尝试单独保留记录。

## 范围与限制

已得到可运行的本地创作工作区。当前保存属于编辑快照，尚无正式稿、模板、模型连接或自动小说生成。
下一阶段 G0003 建立修订与正文/记忆原子提交；真实 Host Dock、窗口关闭行为及完整原生交互验收仍按后续阶段进行。

字段和关键并发设计使用中文注释，存储/生命周期/模型验证已建立专项文档。按用户要求进行独立阶段 Git 提交。
