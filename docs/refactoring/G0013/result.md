# G0013 实施结果

日期：2026-09-12。基线 `1040bf5`，G0012 的 229 项测试。

G0013-01–05 已实现短文字接收、分段覆盖、逐字证据、方法卡、文风维度、章纲小样/A-B 试写、作者编辑批注、
本书/命名模板独立采用、分析/试写独立预算、共享 Tool 任务生命周期与 schema 8 来源记录。

## 验证

方法材料和文风样文均在可控模型与真实 SQLite 下完成提炼 → 小样 → 批注 → 本书/模板复用；
另覆盖伪造/越界证据、源文变化、保存版本冲突、文件类型容量、模型失败检查点、书与 Tool 的任务隔离、关闭草案保存、
保存期间新输入与关闭、渲染长度上限、旧版作品升级。原生输入/证据定位/模板与本书采用已验证。
截图 `artifacts/G0013/ui/material-calibration.png` 已检查，TRX `artifacts/G0013/tests/g0013.trx`。

GPT-6 审查 `artifacts/G0013/codex-review/review.md` 的四项发现已修复并增加回归：并发失败自动换版本覆盖、保存期间新输入被覆盖、
关闭期间漏取消、结构契约与最终规范容量不一致。

实际 Codex GPT-6 首个材料样本返回了提炼对象，但将引用中的“阻碍”改成“障碍”，逐字证据门禁按预期拒绝，未采用。
此次实际用量 9805 输入 + 1160 输出 = 10965 token；记录在 `artifacts/G0013/live-probe`。
补充提示要求保留原用字后，第二个限额样本单独记录；不会把失败样本记为通过。

完整 Host 联调、真实连续样稿与作者质量评测仍属 G0015–G0016。没有 AIFLOW、Windows CI、Release/ZIP 或发布门禁。

第二份短材料成功提炼两张方法卡；随后试写将剧情和方法额外列为约束，因与作者的两条约束不一致而拒绝。
明确提示 Evidence 只对应 Constraints 的原始行后，再发起一次独立试写，成功生成 A 257 字符、B 263 字符，两条约束证据均通过。
这份材料的提炼、被拒试写和成功试写共 36749 token，记录在 artifacts/G0013/live-probe-second 与 artifacts/G0013/live-probe-trial。
成功试写仅验证结构/来源流程，作者文学质量评阅未由本次自动化替代。

最终本地门禁：246 项通过、0 失败、0 跳过；Debug 零警告/错误，格式检查与 247 个文档链接通过。独立开发暂存目录 stage-verified-7014323a0fca4b86887ae0323fa1c4d5 的 SQLite/中文检索探针通过。
