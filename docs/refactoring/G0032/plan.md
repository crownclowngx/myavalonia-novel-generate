# G0032 阶段计划

日期：2026-09-13。基线：`4c45883`，320 项测试通过。状态：工程已验证；真实文件提取同步执行，见[结果](result.md)。

本阶段实施全文运行、预算与中断恢复，保持原生应用分层和单一生产程序集。运行服务只调度，提取服务只构造/解析请求，存储接口只保存检查点。无 AIFLOW、Windows CI 或发布门禁。

| 工作项 | 本阶段实现与验证 |
| --- | --- |
| G0032-01 | 有序依赖节点、同一预算、提取阶段为后续报告保留请求与 token 额度；连接串行与跨进程运行租约 |
| G0032-02 | 操作 ID 先落盘；节点状态与结果同事务；真实 SQLite 故障注入验证响应已记账但节点尚未提交的恢复 |
| G0032-03 | 边界暂停、立即取消、重开核账；截断/失败/未知费用不自动重发，明确恢复保留原费用 |
| G0032-04 | 输入/提示/模型/范围指纹缓存；同源切分修订保留原任务、共享旧预算，复用未变化单元 |
| G0032-05 | 全文覆盖、预算耗尽、限流/超时、重复恢复、独立取消、锁与数据库损坏回归；本地完整门禁 |

真实验证使用用户给出的部分导出 TXT，执行所提供文件全部 20 单元。冻结 `deepseek-flash`、关闭思考、16384 输出上限。全次实验总限额 80 请求/300 万保守预留 token；扣除 G0031 的 6 次/200862 后，本运行最多 74 次/2799138，其中为后续阶段保留 32 次/120 万。一次性凭据只进入进程内存。

本阶段完成态明确为“全文提取完成”，不代表六专题与综合报告已完成。后续使用同一预算的修订运行复用提取成果。
