# G0032 阶段结果

日期：2026-09-13。状态：工程验证完成，阶段 5/5 项；真实文件提取同步执行。基线 `4c45883`。

已实现 G0032-01–05 的运行、共享预算、检查点事务、暂停/取消、费用复核、切分修订及缓存职责。来源与配置固定，旧运行保留；恢复不自动重发不确定请求，完整账本可补偿节点事务中断。

最终本地完整门禁 340 项测试全部通过，0 失败、0 跳过，编译 0 警告/错误；格式、409 个本地文档链接、手册源哈希与 Git 空白检查通过。新增运行专项 19 项及同维多缺口回归 1 项。专项测试使用真实 SQLite 触发器注入节点结果写入失败，验证事务回滚和从账本重放；覆盖发送前窗口、明确重试、版本缓存、同源切分及累计费用、跨实例租约、外来/未来库和显示回调失败。

真实 TXT 的首次运行处理到 3/20 单元后，第 4 单元把 `CharacterClaim` 错填到只接受 Explicit/Inferred/Uncertain 的 Kind 字段。契约拒绝，原响应和已知 10465 token 保留；另外三次已知用量分别为 4854、13451、13894。此时总用量为 42664，未发生自动重发。人工核对失败字段后，已在同一预算明确重试，后续进展待补充。

本阶段只验收全文提取与恢复基础，不宣称全书综合分析或真实小说语义质量通过。M1 的确定性队列基础已具备；真实文件完整提取结果及后续整合报告分别继续记录，最终 M3 仍需 G0036 的人工与 Host 验收。

专项契约见[恢复与预算](run-recovery-contract.md)，职责见[实施方案](implementation.md)。

2026-09-13 补充：首次明确恢复后完成 6/20 单元，第 7 单元再次把 CharacterClaim 填入 Kind；累计 8 次请求、80626 个已知 token。修订为提示 v3，分别列出确定性/叙述来源/实体类别的允许值与例子；旧提示 v2 保留供历史运行验证。新运行从缓存采纳 6 个有效节点，未重发这些章节，剩余单元使用 v3；预算 ID 与全部费用保持原值。

第二轮修订进展至 8/20 时出现第三次拒绝：Gaps 对同一维度给出了不同的多个缺口，总计 8 条。复核确认“每维只能一条”属于不必要的本地约束，已兼容调整为最多 24 条且每条 1000 字符，保留全部原始说明；不改变 JSON 字段结构，不修补或猜测内容，因此保留 v2 契约输入身份。已加同维多缺口与超限回归。此时累计 11 次请求、117766 已知 token；失败账本不改写，后续仍需明确复核重试。

提交前进展：同一预算继续到 13/20 单元，已完成节点正常持久化；完整文件最终统计在完成后追加，当前不宣称全文实测已结束。
