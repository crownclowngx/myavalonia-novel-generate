# G0031 实施结果

日期：2026-09-13。状态：阶段已验收（单元提取与协议；非全书质量验收）。基线：`1dcbd90`，313 项测试。

G0031-01–05 完成：多维 JSON 契约、统一请求账本、来源指纹、段号证据、稳定结果身份、截断与费用保护，以及受控测试和 DeepSeek Flash 真实章节小样。实际方案见[实施说明](implementation.md)，当前契约见[提取契约 v2](chunk-extraction-contract.md)。

## 实际验证

- 新增提取测试 5 项、模型协议测试 2 项；相关提取/协议组合 45 项通过。
- 最终 `tools/verify-local.ps1` 通过：锁定还原、Debug 0 警告/0 错误、320 项测试且 0 失败/0 跳过、格式、文档链接/HTML 来源哈希、Git 空白检查。
- 实测账户 `/models` 返回 `deepseek-flash` 和 `deepseek-v4-pro`；实际章节验证使用 Flash、关闭思考、16384 输出上限。
- 用户 TXT 的第 2 个分析单元（4141 字符正文）成功提取 26 个实体提及、19 条观察、6 项信息缺口；所有引用按段号从原文生成，并通过来源和区间验证。已有观察与该维度的其他未知项可以并存。
- 成功调用用量：输入 7939、输出 5506，总计 13445 token；耗时约 24.8 秒。本机结果为 `artifacts/G0031/deepseek-chunk-06/chunk-analysis.json`。

## 失败证据与成本

共执行 6 次生成请求（含小型协议探针），另有一次只读模型列表查询。首次模型标识尝试及两次思考模式章节请求未形成可用正文；原适配器未保留其返回用量，三条请求仍按未知记录，不猜测回填。第四份章节尝试取得 JSON，但原文抄写错误被 v1 严格校验拒绝。

最终 v2 改用“模型引用段号、程序取回原文”，不自动纠正模型抄写的错字。输入保留可读中文，减少 Unicode 转义开销。空截断现在可正确保存 Truncated 与有效用量；完整但空的响应仍拒绝。

六次请求累计已知用量 34671 token，三条未知用量保守预留 166191 token，合计已知/保守预留 200862 token；均计入本次总预算，失败记录不删除。各次账本和用量位于 `artifacts/G0031` 的独立目录。

未在源码、文档、夹具或工具文本中发现实际密钥格式字面量；会话凭据在探针退出时清除，未使用持久凭据文件。未运行 Windows CI、发布门禁或部署。

## 下一步

进入 G0032 的全文节点调度、预算、缓存和恢复。G0031 成功只说明一个单元的结构与来源契约可工作，不能证明整本语义准确率。累计 3/8 阶段、15/40 工作项；M1/M2/M3 均未达到。
