# G0012 阶段计划

基线 `b1c1019`，G0011 的 211 项测试与 Codex GPT-6 单章工作稿闭环通过。

实现 3–5 章连续运行、共享预算、同书单任务、单连接串行、暂停/取消/中断检查点及明确继续。
优先复用 PlanningService、ChapterGenerationService 与原子提交；不增加通用工作流框架。
检查点恢复须识别“提交已完成但运行日志未更新”，不能重复生成或重复采用；预算增加与不确定用量必须经作者明确动作。

使用本地 Debug、故障模型与 SQLite 验证，更新专项文档并阶段提交；不使用 AIFLOW、Windows CI 或发布门禁。
