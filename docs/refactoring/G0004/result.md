# G0004 实施结果

日期：2026-09-12。状态：本地实现与阶段验证完成，真实 Host 待联调。

- G0004-01：身份、草案、不可变版本、标签、来源及四维规范已实现。
- G0004-02：共享模板 Tool 的编辑、查找、复制、归档及草案恢复已实现。
- G0004-03：新身份作品可按模板及所选维度创建，失败不创建作品文件；本书快照独立持久化。
- G0004-04：采用前整维度差异预览、预览过期保护与本书另存模板已实现。
- G0004-05：两书隔离、归档/源删除、模块单例、View 重建及关闭竞争已有本地回归。

## 实际验证

本地入口 `./tools/verify-local.ps1` 执行锁定还原、Debug 零警告构建、测试、格式与文档链接检查。
本阶段 55 项测试通过，无跳过；包含前三阶段回归。原生控件截图位于 `artifacts/G0004/ui/`，
TRX 位于 `artifacts/G0004/tests/`。截图已检查模板输入、滚动区域与状态栏。

Codex CLI 0.153.4 使用既有 ChatGPT 登录调用 `gpt-6-astra` 审查指定源码。
发现关闭保存代数、创建发布原子性、缺失资产恢复文件三项问题，均已修复并加入回归。
另增加模板数据列与快照不一致的损坏保护。模型报告与事件保留在忽略的 `artifacts/G0004/codex-review/`。

## 限制

未进行真实 Host Dock/卸载/退出联调；预览页和 Headless 的视图重建不等于 Host 验收。
没有运行 AIFLOW、Windows CI、Release、ZIP 或发行门禁。尚无 AI 生成，不能把 G0004 完成视为首版产品完成。
模板采用目前为整维度明确替换；逐行合并和模型提炼由后续阶段承担。

阶段按 Git 独立提交，下一入口为 G0005 模型连接与独立凭据。
