# G0010 分层规划与近期章纲实施计划

基线：`a14cebd`（G0009），164 项本地测试通过。
关联 [总计划](../../implementation-plan.md#g0010)。

1. G0010-01：最小创意输入，默认规划当前章起 3 章，可选择 3–5 章；复用本书模板、规则和实体。
2. G0010-02：结构化生成主线、分卷目标、近期章节目标/冲突/视角/时地/事件/变化/伏笔/承接及实体建议。
3. G0010-03：写作方法必须落到具体铺设、阻碍和兑现事件，并保留来自本书方法原文的引用。
4. G0010-04：自动模式直接保存合法规划；协作模式保留候选供选择采用。支持当前章局部手工修改、锁定和追加规划历史。

已有正文及修订不可被规划覆盖。无效结构、过期请求、规则冲突或目标章锁定在请求/采用边界明确拒绝。
模型访问统一经 G0008，使用 Codex GPT-6 做有限实际结构输出验证；常规门禁不调用远程模型。
保持 SOLID、中文注释与专用规划契约。不使用 AIFLOW、Windows CI 或发布门禁；验证后独立提交。
