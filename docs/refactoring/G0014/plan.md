# G0014 阶段计划

日期：2026-09-12。基线 `7b4657b`，246 项本地测试。

按 G0014-01–04 接入选区改写、末尾续写、手写复核、正文差异、历史审阅、连续范围定稿与保守影响清单。
复用修订账本和单章审校，不建立多分支合并框架。接受前核对作品、正文、父修订与规范，批量定稿必须全部通过才落库。

验证覆盖实际 SQLite、模型故障夹具、原生输入及版本导出；保留 Codex GPT-6 审查结果。
不使用 AIFLOW、Windows CI、Release、ZIP 或发布门禁。
