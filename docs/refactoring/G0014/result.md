# G0014 实施结果

日期：2026-09-12。基线 `7b4657b`。

G0014-01–04 已实现选区改写、末尾续写、手写重新审校、候选差异与放弃、历史对比、连续范围定稿、影响清单及选版审校提示。
增加 schema 9 和全报告规范指纹，旧版 Passed 不自动升级成当前有效报告。

## 验证记录

覆盖选区外围不变、半字符边界、失败不扩大改写、零事实审校过期、同文不同父修订、迟到采用拒绝、批量末章失败无半提交、
实际 SQLite 重开与迁移前备份、所选正式版本的导出提示。原生选区 → 候选对比 → 明确提交 → 历史对比 → 连续定稿通过。

GPT-6 审查发现导出审校提示混入编辑稿状态，已按所选版本修复并增加回归。审查记录 artifacts/G0014/codex-review/review.md。
原生测试发现 SelectionStart/End 缺少回传，已显式双向绑定并验证输入。

完整 Host 交互与真实连续样稿仍在 G0015–G0016；没有执行发布门禁。

最终本地门禁：262 项通过、0 失败、0 跳过；Debug 零警告/错误，格式和 255 个文档链接通过。独立暂存目录 stage-verified-21c8a14031074017aff3ee5f73fdbcfc 的 SQLite/中文检索探针通过。
