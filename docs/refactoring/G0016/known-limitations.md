# 首版工程候选的已知限制

日期：2026-09-12。结论：**工程候选已验证，P1 产品验收未完成。**

1. 真实三章工作稿已生成；作者文学质量签收尚未完成，500 字短章不能代表长篇质量。
2. 真实 Host 组合/Scope/View 适配通过；系统文件选择器、完整 Dock 与真实 IME/多屏 DPI 仍缺桌面交互证据。
3. DeepSeek 依据官方协议实现并通过受控 HTTP/SSE 验证；未使用真实 DeepSeek Key 验证远端。本阶段模型实测全部走 Codex GPT-6。
4. 首版按近期 3–5 章工作流验证，一个活动运行，不提供通用多分支合并；长篇精细依赖、时态知情和检索增强属于 G0017–G0020。
5. 改前文会保守提示后续范围。存在后续工作稿时先放弃本轮工作稿，正式稿先回退相关范围，再逐章复核；不会自动改正式正文。
6. 材料入口只支持有界短文本、TXT、Markdown；不含 PDF/PPT/OCR、整本参考小说分析、EPUB/DOCX 导出或 Workflow Provider 扩展。
7. Codex 调用包含 CLI 上下文开销，实际三章报告为 90187 token；不承诺费用等同输出正文 token。未知费用仍按保守预留处理，明确继续才允许新增请求。
8. schema 9 打开旧项目前创建备份；旧 Passed 缺少规范指纹时需复核，不自动补造证明。项目文件不含明文密钥，连接凭据独立管理。
9. 本次没有正式发行包、远程推送或发布，未运行 Windows CI、Release/ZIP 发布门禁，也未使用 AIFLOW。
