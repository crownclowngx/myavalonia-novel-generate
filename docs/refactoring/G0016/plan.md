# G0016 阶段计划

日期：2026-09-12。基线 `af373c3`，G0015 的 270 项本地测试已通过。

按 G0016-01–05 汇总 A01–A16，补齐 HTTP/SSE 到连续创作/SQLite 的组合回归，跑真实 Codex GPT-6 三章样稿，
验证当前 Host 的 Provider、注册表、Scope、Dock View 适配和关闭，最后同步产品文档、HTML、操作说明及限制。

本次样稿明确采用人工编排三章输入，每章 500 字，最多一次修正，总预算 12 次请求 / 400000 token；不会自动定稿。
这不替代 G0010 的真实模型规划证据，也不把模型自评充当作者文学质量签收。
系统选择器、完整桌面 Dock 与作者签收缺少证据时保留待验；非发布任务不运行 Windows CI 或发布门禁。
