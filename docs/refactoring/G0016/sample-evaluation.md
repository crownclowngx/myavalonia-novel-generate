# G0016 真实三章样稿评测

日期：2026-09-12。源基线 `af373c3`；本阶段后续只修正 Host 菜单身份和验收工具/文档，生成核心未变。

## 输入与预算

题目《雨夜未寄出的信》，单一出场人物林舟；雨夜进入旧邮局 → 读十年前给自己的信 → 写回信，现实故事。
三章人工验收章纲包含期待、阻碍、兑现方法事件；不是本轮模型生成的章纲。真实模型规划的独立证据见 G0010。
连接：既有 Codex 套餐，CLI 0.153.4，`gpt-6-astra`，low，输出上限 8192。未读取认证缓存、未复制登录文件。
每章目标 500 非空白 Unicode 字符（含标点），容差 ±20%，最多 1 次局部修正；总计最多 12 次请求、400000 token，20 分钟停止期限。

## 实际结果

| 章节 | 非空白字符 | 已通过审校的事实增量 | 修正 |
| --- | --- | --- | --- |
| 雨中的旧门 | 487 | 5 | 0 |
| 抽屉里的年月 | 498 | 7 | 0 |
| 回信 | 477 | 8 | 0 |

连续运行 Completed，3/3 工作稿，6 次请求，输入 85665 + 输出 4522 = **90187 token**；未知用量 0。
正文、摘要、事实原子保存，正式稿 0。这里记录的是 CLI 报告的总用量，包含其运行上下文，不能只按小说字数估算成本。

可阅读[三章工作稿](samples/rain-letter-working-draft.md)与[机器可读元数据](samples/sample-metadata.json)。
原始输入、逐次请求、完整运行检查点、项目数据库在 artifacts/G0016/live-probe/bin/Debug/net10.0；哈希已归档。
元数据中的哈希针对原始输出文件；文档内的工作稿副本仅清理文件末尾空行并遵循仓库换行规范，正文未改写。
`.noveldb` 可以离线打开。该样本连接位于隔离的验收目录，打开后要继续收费生成，需在本书明确绑定可用连接。

## 阅读备注与待评阅项

本助手通读后观察：三章按门锁、抽屉、回信连续推进，旧信裂口在第三章被承接，未增加出场人物，结尾收束。
第二章“磨擦声”可由作者考虑改成“摩擦声”；风格是否偏好、情绪层次是否足够，不能由结构和事实门禁决定。

这是约 1462 字的短篇流程样本，不证明长篇质量或常见 2000–3000 字网文章节效果；本次无修正的结果也不代表一般情况下不需返工。
作者文学质量评分、可接受修改量及“愿意继续使用”的反馈尚未取得，**不能签收 P1 产品验收**。
待作者记录：剧情承接/人物一致性/方法落地/文风偏好/修改量，每项给出正文例子和结论。

## 显式重跑工具

```powershell
dotnet run --project tools/LiveSample/LiveSample.csproj -c Debug -- C:/path/to/codex.exe D:/new-empty-sample-directory --run-paid-sample
```

该命令会使用当前登录套餐，必须选择空输出目录。普通 verify-local.ps1 不会调用它；代码留档用于复现，不自动重复本次收费样本。
