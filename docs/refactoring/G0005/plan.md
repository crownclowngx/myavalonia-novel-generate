# G0005 模型连接与独立凭据管理

基线：G0004 `43a7c10`，55 项本地测试通过。

实施 G0005-01 至 G0005-04：连接身份与配置版本、规划/正文/检查预设、新书默认建议、本书显式绑定、
请求配置冻结、共享连接 Tool、会话密钥与 Windows CurrentUser DPAPI 加密文件。

默认开发路径是 Codex CLI + gpt-6-astra，复用 CLI 已登录套餐；用户明确选择可执行文件，插件不读取认证缓存。
DeepSeek 配置与 Key 单独管理，不与 Codex 认证混用。本阶段只做配置和本地校验；远程协议在 G0008 实现。

使用真实临时数据库与 DPAPI 验证隔离、重启、清除、端点变化、损坏及不带秘密的作品快照。
不使用 AIFLOW、Windows CI 或发布门禁；完成后独立提交并同步专项文档。
