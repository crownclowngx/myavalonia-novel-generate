# G0005 实施结果

日期：2026-09-12。状态：本地实现与阶段验证完成；远程协议属于 G0008。

- G0005-01：稳定连接身份、版本、三个任务预设、新书默认建议、本书版本绑定与无秘密冻结对象已实现。
- G0005-02：会话凭据和 Windows 当前用户加密保存、替换、清除、不可用状态与短暂输入已实现。
- G0005-03：共享模型连接 Tool 与 Document 的查询/明确绑定/解绑已实现，无 Key 可离线编辑。
- G0005-04：同名双连接、端点变更、旧授权迟到写入、搬迁重绑、密文损坏、清除和非秘密快照已验证。

## 验证

本地 73 项测试通过、无跳过，包含真实 DPAPI 与 SQLite；TRX 在 `artifacts/G0005/tests/g0005.trx`。
原生控件中文输入、三个任务的数值绑定和密钥遮蔽验证通过；已检查截图 `artifacts/G0005/ui/model-connections.png`。
`./tools/verify-local.ps1` 完成锁定还原、Debug 零警告构建、全量本地测试、格式与文档链接检查。

GPT-6 Astra 文本审查发现三个并发问题：旧凭据覆盖新版、保存期间新输入丢失、异步状态串线，均修正并新增定向回归。
报告存于忽略目录 `artifacts/G0005/codex-review/`。新增依赖正确进入 Debug 私有暂存，独立 ALC SQLite 探针通过。

没有使用 AIFLOW、Windows CI 或发布门禁，没有 Release/ZIP。没有进行真实 Host 联调、DeepSeek 请求或小说生成。
历史凭据槽位的物理清理尚未提供 UI，当前版本不会重新选择旧授权。具体边界见 [专项契约](connection-and-credential-contract.md)。

阶段独立提交后进入 G0006 长期规则与确定性检测基础。
