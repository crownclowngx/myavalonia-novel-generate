# 2026-09-13 分析保存与报告转模板本机部署

本轮版本为 **1.2.0**，源码提交 `301ad4d`，包含 G0040 结构化契约诊断及 G0041–G0044 已实现的分析保存、报告转模板和作品采用。状态：Release 构建、测试、制品与存储检查通过，等待正常退出运行中的宿主再替换。

目标为 `D:\data\avalonia\Controls\NovelGeneratePlugin`。配套 Host 关闭修复需要额外更新 `D:\data\avalonia` 的主程序文件；其构建和验证已经完成，部署范围等待本轮用户答复。

## 验证与制品

- 插件 locked restore、Release 构建及 441 项 Release 测试通过，零警告/错误、零失败/跳过。
- 通过 `BuildManagedPluginPackage` 从独立源码快照生成正式 ZIP 与外置摘要，解压后保留 11 个插件私有资产；不直接复制普通 bin 输出。
- 暂存包实际程序集版本为 1.2.0.0。独立 ALC 加载、入口类型、SQLite 托管/原生依赖、中文作品往返、分析运行库和报告转模板库检查通过，全部使用新建合成数据。
- Host 源码 `88b4d8b` 包含退出前排空与 Dock 迟到刷新修复，完整开发 Gate 813 项通过。Release win-x64 自包含压缩单文件 EXE 的独立数据目录启动/退出检查通过，退出码 0；该启动检查没有装载用户插件。

插件 DLL SHA-256：`3F85271BC9623B4741B4411E8B3B3EA3BCA8B7F1D4084444E2897F0DD31A88F3`。

ZIP SHA-256：`84586368DC120B2A8A61477909611C984C5C9349404D7A840F0A0C2228ECBD40`。

本机证据目录：`artifacts/deployments/20260913-204431-report-template/`。包含 Release 日志、TRX、ZIP/外置摘要、staged 目录及经过路径校验的部署和存储探针。部署脚本在宿主仍运行时拒绝替换，替换前备份旧插件，替换后逐文件核对摘要及其他插件不变。

## 如何导入和采用

### 已经分析过的小说

1. 启动宿主，从工具中心打开“创作模板库”，展开“整本小说分析”。
2. 选择已保存的小说和运行，在“报告”点击“读取所选运行的报告”。旧记录可通过“加载更早的已保存运行”找回；读取不调用模型。
3. 完整报告内展开“从报告生成模板”，填写模板名称、选择模型连接，勾选世界观、文风、写作方法及需要的长期规则，设置本次请求/token 预算。
4. 点击“预览转换计划”，确认后“生成新模板草案”。转换会另行调用模型，但不重新分析全文；任务和成果自动保存。
5. 完成后“打开生成草案”，核对来源及规范，编辑后“保存新版本”。若当前有未保存的其他模板，先处理那份编辑；新草案已经保存在库中，无需重新生成。
6. 打开或创建作品，在“设定 → 本书规范与模板”刷新模板、选择刚保存的版本，勾选要采用的维度，先“预览采用差异”，再“采用此版本”，最后保存作品。各作品保存独立快照。

### 第一次导入 TXT

在“整本小说分析 → 导入”选择 TXT，点击“预览全文”，核对编码和分章后“确认导入”。再到“运行”选小说、模型连接及预算，点击“开始新的全文分析”。完整报告生成后按上方步骤转成模板。外部 TXT 移动或删除不会影响已经保存的来源快照。

“分析导入”用于参考分析与模板提炼，当前不会自动把外部 TXT 变成本书的创作章节，也不会把原书人物经历直接建成新书人物档案。

## 验收边界

本次不复用以前的一次性 Key、不自动发起任何真实模型请求。G0044-04 的真实模型转换和作者内容评阅仍待验，[改造进度](../novel-analysis-template-conversion-plan.md#progress)保持 19/20。部署完成不等同于真实内容质量已经验收。
